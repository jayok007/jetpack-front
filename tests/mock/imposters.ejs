{
  "imposters": [
    {
      "port": 4545,
      "protocol": "http",
      "stubs": [
        {
          "predicates": [
            { "equals": { "method": "GET", "path": "/jetpacks" } }
          ],
          "responses": [
            { "inject": "<%- stringify(filename, 'getJetpacks.ejs') %>" }
          ]
        },
        {
          "predicates": [
            { "equals": { "method": "POST", "path": "/jetpacks" } }
          ],
          "responses": [
            { "inject": "<%- stringify(filename, 'createJetpack.ejs') %>" }
          ]
        }
      ]
    }
  ]
}